# Plan Archive

This directory stores historical Superpowers implementation plans and long-form background plans.

The active execution dispatcher is `docs/NOW.md`. The ordered queue is `docs/ROADMAP.md`.

Rules:

- Treat checked-off Python plans as completed history.
- Treat `2026-06-03-autolight-rust-cxx-qt-port.md` as the background migration plan, not as the per-session dispatcher.
- For new work, create or update a small batch in `docs/NOW.md` instead of adding another long implementation transcript here.
